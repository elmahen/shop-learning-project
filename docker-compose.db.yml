services:
  postgres:
    image: postgres:16-alpine
    container_name: shop_db
    environment:
      POSTGRES_DB: shopdb
      POSTGRES_USER: shop_user
      POSTGRES_PASSWORD: shop_pass
    ports:
      - "5432:5432"
